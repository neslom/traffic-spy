<h3>URL list in order of reoccurance:</h3>
<ol>
  <% @sorted_urls.each do |identifier_url| %>
    <% x = "/sources/#{params[:identifier]}/urls#{URI.parse(identifier_url).path}" %>
  <li><a href="<%= x %>"><%= identifier_url %></a></li>
<% end %>
</ol>
